<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
  <meta charset="utf-8"/>
  <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
  <title>لوحة الإدارة - وضع الصيانة</title>
  <script src="/theme.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Aref+Ruqaa:wght@400;700&family=Tajawal:wght@300;400;500;700;900&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <script>
    tailwind.config = { darkMode: "class", theme: { extend: {
      colors: { primary: "#005A2B", primaryDark: "#00361A", accent: "#E5B869", "bg-light": "#F5F3F7", "bg-dark": "#050505", "card-light": "rgba(255, 255, 255, 0.8)", "card-dark": "rgba(15, 12, 25, 0.8)", "border-light": "rgba(0, 90, 43, 0.1)", "border-dark": "rgba(229, 184, 105, 0.1)" },
      fontFamily: { display: ["'Aref Ruqaa'", "serif"], sans: ["Tajawal", "sans-serif"] }
    }}};
  </script>
  <style type="text/tailwindcss">@layer utilities { .glass-panel { @apply backdrop-blur-2xl shadow-xl; } }

    /* ===== Responsive admin layout: mobile + tablet ===== */
    @media (max-width: 1279px) {
      body {
        height: auto !important;
        min-height: 100dvh !important;
        overflow-x: hidden !important;
        overflow-y: auto !important;
        display: block !important;
      }
      main {
        width: 100% !important;
        min-height: 100dvh !important;
        display: block !important;
      }
      main > header {
        position: sticky !important;
        top: 0 !important;
        z-index: 60 !important;
        padding: 12px !important;
        gap: 10px !important;
        flex-wrap: wrap !important;
        align-items: flex-start !important;
        background: rgba(245, 243, 247, 0.88) !important;
        backdrop-filter: blur(18px) !important;
        border-bottom: 1px solid rgba(0, 90, 43, 0.08) !important;
      }
      html.dark main > header {
        background: rgba(5, 5, 5, 0.86) !important;
        border-bottom-color: rgba(229, 184, 105, 0.12) !important;
      }
      main > header h2 {
        font-size: 1.35rem !important;
        line-height: 1.25 !important;
      }
      main > header > div:first-child {
        min-width: 0 !important;
        flex: 1 1 auto !important;
      }
      main > header > div:last-child {
        width: 100% !important;
        justify-content: flex-start !important;
        overflow-x: auto !important;
        padding-bottom: 2px !important;
      }
      main > header > div:last-child a,
      main > header > div:last-child button {
        flex: 0 0 auto !important;
      }
      .flex-1.overflow-y-auto {
        overflow: visible !important;
        padding-inline: 12px !important;
        padding-bottom: 28px !important;
      }
      .max-w-\[100rem\], .max-w-7xl, .max-w-5xl {
        width: 100% !important;
        max-width: 100% !important;
      }
      .grid {
        gap: 12px !important;
      }
      .rounded-\[2rem\], .rounded-3xl {
        border-radius: 1.25rem !important;
      }
      #mobileNavDrawer {
        right: auto !important;
        left: 0 !important;
        width: min(340px, 88vw) !important;
        transform: translateX(-115%) !important;
      }
      #mobileNavDrawer.isOpen {
        transform: translateX(0) !important;
      }
    }

    @media (max-width: 640px) {
      main > header { padding: 10px !important; }
      main > header h2 { font-size: 1.18rem !important; }
      main > header .uppercase { display: none !important; }
      .w-12.h-12 { width: 44px !important; height: 44px !important; }
      table { font-size: 12px !important; }
    }


    /* ===== FINAL mobile/tablet admin drawer fix ===== */
    @media (max-width: 1279px) {
      #mobileNavBtn {
        position: relative !important;
        z-index: 100002 !important;
        pointer-events: auto !important;
        cursor: pointer !important;
        touch-action: manipulation !important;
      }
      #mobileNavDrawer {
        display: flex !important;
        position: fixed !important;
        top: 0 !important;
        bottom: auto !important;
        left: 0 !important;
        right: auto !important;
        width: min(360px, 88vw) !important;
        max-width: 88vw !important;
        height: 100dvh !important;
        max-height: 100dvh !important;
        z-index: 100001 !important;
        visibility: visible !important;
        opacity: 1 !important;
        pointer-events: none !important;
        transform: translate3d(-120%, 0, 0) !important;
        transition: transform .32s cubic-bezier(.2,.8,.2,1) !important;
        border-left: 0 !important;
        border-right: 1px solid rgba(229,184,105,.22) !important;
      }
      #mobileNavDrawer.admin-menu-open {
        transform: translate3d(0, 0, 0) !important;
        pointer-events: auto !important;
      }
      #mobileNavOverlay {
        display: none !important;
        position: fixed !important;
        inset: 0 !important;
        z-index: 100000 !important;
        background: rgba(0,0,0,.62) !important;
        backdrop-filter: blur(7px) !important;
      }
      #mobileNavOverlay.admin-menu-open {
        display: block !important;
      }
      body.admin-menu-lock {
        overflow: hidden !important;
        touch-action: none !important;
      }
    }
</style>
  <style>
    ::-webkit-scrollbar { width: 6px; height: 6px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: #005A2B; border-radius: 10px; }
    html.dark ::-webkit-scrollbar-thumb { background: #E5B869; }
    #nexusCanvas { position: fixed; inset: 0; width: 100%; height: 100%; z-index: 0; pointer-events: auto; }
    html.dark .dark-toggle-icon-moon { display: none; }
    html:not(.dark) .dark-toggle-icon-sun { display: none; }
    #mobileNavOverlay.hidden { display: none; }
    #mobileNavOverlay { display: block; }
    #mobileNavDrawer { transform: translateX(110%); transition: transform .4s cubic-bezier(0.16,1,0.3,1); }
    #mobileNavDrawer.isOpen { transform: translateX(0); }
    .field-box:focus { box-shadow: 0 0 0 4px rgba(0, 90, 43, .10); }
    html.dark .field-box:focus { box-shadow: 0 0 0 4px rgba(229, 184, 105, .12); }
  </style>
  <script>
    (function () {
      const saved = localStorage.getItem("theme") || "dark";
      document.documentElement.setAttribute("data-theme", saved);
      document.documentElement.classList.toggle("dark", saved === "dark");
    })();
  </script>
</head>
<body class="bg-bg-light dark:bg-bg-dark text-gray-800 dark:text-gray-200 font-sans h-screen flex overflow-hidden selection:bg-accent selection:text-primaryDark">
  <canvas id="nexusCanvas"></canvas>
  <div class="fixed inset-0 bg-gradient-to-b from-white/40 to-white/80 dark:from-black/40 dark:to-[#050505]/90 pointer-events-none z-[1]"></div>

  <aside class="relative z-20 hidden xl:flex flex-col w-[280px] h-[calc(100vh-2rem)] my-4 mx-4 bg-card-light dark:bg-card-dark glass-panel rounded-3xl border border-border-light dark:border-border-dark overflow-hidden shrink-0">
    <div class="flex items-center gap-3 p-6 border-b border-border-light dark:border-border-dark shrink-0">
      <div class="w-12 h-12 flex items-center justify-center rounded-2xl bg-primary dark:bg-accent text-white dark:text-black shadow-lg"><span class="material-symbols-outlined text-[28px]">shield_person</span></div>
      <div><h1 class="font-display font-bold text-xl leading-none text-primary dark:text-white">عائلة خاشقجي</h1><p class="text-[10px] font-bold text-gray-500 dark:text-accent uppercase tracking-widest mt-1.5">نظام الإدارة المركزية</p></div>
    </div>
    <nav class="flex-1 overflow-y-auto p-4 space-y-6">
      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">البيانات الرئيسية</p>
        <a href="/admin" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">group</span> إدارة الأفراد</a>
        <a href="/admin/pages" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">history_edu</span> إدارة الصفحات</a>
        <a href="/admin/honor" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">workspace_premium</span> السير الذاتية</a>
        <a href="/admin/news" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">newspaper</span> الأخبار والمناسبات</a>
      </div>
      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">النظام والموقع</p>
        <a href="/admin/dashboard" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">dashboard</span> لوحة الإحصائيات</a>
        <a href="/admin/activity" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">manage_history</span> سجل النشاطات</a>
        <a href="/admin/maintenance" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-bold text-sm border border-primary/20 dark:border-accent/20 transition-all relative overflow-hidden shadow-sm"><div class="absolute right-0 top-1/2 -translate-y-1/2 w-1.5 h-6 rounded-l-full bg-primary dark:bg-accent"></div><span class="material-symbols-outlined text-[22px]">construction</span> وضع الصيانة</a>
        <a href="/admin/support-messages" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">mail</span> رسائل الدعم</a>
        <a href="/" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent"><span class="material-symbols-outlined text-[22px]">account_tree</span> عرض الشجرة</a>
      </div>
    </nav>
    <div class="p-4 border-t border-border-light dark:border-border-dark shrink-0 m-2 rounded-2xl bg-white/50 dark:bg-black/30">
      <div class="flex items-center gap-3"><div class="w-10 h-10 rounded-full bg-primary/10 dark:bg-accent/10 flex items-center justify-center text-primary dark:text-accent border border-gray-200 dark:border-white/10 shadow-sm"><span class="material-symbols-outlined text-[20px]">person</span></div><div class="flex-1 min-w-0"><p class="text-sm font-bold text-gray-900 dark:text-white truncate"><%= admin?.username || "مدير النظام" %></p><p class="text-[10px] text-primary dark:text-accent uppercase tracking-widest mt-0.5 font-bold">Admin</p></div><form method="post" action="/admin/logout" class="m-0"><button type="submit" class="w-9 h-9 flex items-center justify-center rounded-xl bg-red-50 dark:bg-red-500/10 text-red-500 hover:bg-red-500 hover:text-white transition-all shadow-sm" title="تسجيل خروج"><span class="material-symbols-outlined text-[18px]">logout</span></button></form></div>
    </div>
  </aside>

  <main class="flex-1 flex flex-col min-w-0 h-full relative z-10">
    <header class="flex items-center justify-between p-4 lg:p-8 shrink-0">
      <div class="flex items-center gap-3"><button id="mobileNavBtn" onclick="return window.openAdminMobileMenu ? window.openAdminMobileMenu(event) : false" class="xl:hidden flex items-center justify-center w-12 h-12 rounded-2xl bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-primary dark:text-accent shadow-sm" type="button"><span class="material-symbols-outlined text-[24px]">menu</span></button><div><h2 class="text-2xl md:text-3xl font-display font-bold text-primary dark:text-white tracking-tight leading-none mb-1.5">وضع الصيانة</h2><div class="flex items-center gap-1.5 text-gray-500 text-[11px] font-bold uppercase tracking-widest"><span>لوحة الإدارة</span><span class="material-symbols-outlined text-[14px]">chevron_left</span><span>النظام والموقع</span><span class="material-symbols-outlined text-[14px]">chevron_left</span><span class="text-accent">الصيانة</span></div></div></div>
      <div class="flex items-center gap-3 shrink-0"><button id="themeBtn" class="flex items-center justify-center w-12 h-12 rounded-2xl bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-gray-600 dark:text-accent shadow-sm hover:-translate-y-1 transition-transform" type="button" title="تبديل المظهر"><span class="material-symbols-outlined text-[22px] dark-toggle-icon-moon">dark_mode</span><span class="material-symbols-outlined hidden dark:block text-[24px]">light_mode</span></button><a href="/admin/dashboard" class="flex items-center gap-2 px-6 h-12 rounded-2xl font-bold text-sm bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-primary dark:text-accent shadow-sm hover:-translate-y-1 transition-transform"><span class="material-symbols-outlined text-[22px]">dashboard</span><span class="hidden sm:inline">الإحصائيات</span></a></div>
    </header>

    <div class="flex-1 overflow-y-auto px-4 lg:px-8 pb-12">
      <div class="max-w-[100rem] mx-auto space-y-8">
        <% if (saved) { %>
          <div class="bg-green-100 dark:bg-green-500/10 text-green-800 dark:text-green-300 border border-green-200 dark:border-green-500/20 rounded-2xl px-5 py-4 font-bold">تم حفظ إعدادات الصيانة بنجاح.</div>
        <% } %>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-4 lg:gap-6">
          <div class="bg-card-light dark:bg-card-dark glass-panel rounded-[2rem] border border-border-light dark:border-border-dark p-5 flex items-center gap-4 shadow-sm lg:col-span-2">
            <div class="w-16 h-16 rounded-2xl <%= enabled ? 'bg-amber-100 dark:bg-amber-500/20 text-amber-700 dark:text-amber-300' : 'bg-green-100 dark:bg-green-500/20 text-green-700 dark:text-green-300' %> flex items-center justify-center"><span class="material-symbols-outlined text-[30px]">construction</span></div>
            <div><p class="text-[11px] font-bold text-gray-500 uppercase tracking-widest mb-0.5">حالة الموقع العامة</p><p class="text-2xl font-black text-primary dark:text-white leading-none"><%= enabled ? 'وضع الصيانة مفعل حالياً' : 'الموقع يعمل بشكل طبيعي' %></p><p class="text-sm text-gray-500 dark:text-gray-400 mt-2">يمكن للإدارة متابعة العمل والدخول للوحة التحكم أثناء تفعيل وضع الصيانة.</p></div>
          </div>
          <div class="bg-card-light dark:bg-card-dark glass-panel rounded-[2rem] border border-border-light dark:border-border-dark p-5 shadow-sm">
            <p class="text-[11px] font-bold text-gray-500 uppercase tracking-widest mb-3">معلومة سريعة</p>
            <div class="rounded-2xl p-4 bg-white/60 dark:bg-white/5 border border-black/5 dark:border-white/10">
              <p class="font-black text-primary dark:text-white mb-2">صفحة الصيانة العامة</p>
              <p class="text-sm text-gray-600 dark:text-gray-300 leading-7">الزائر العادي سيرى رسالة الصيانة فقط، بينما الإدارة تستمر بالوصول للموقع لإجراء التعديلات والاختبارات.</p>
            </div>
          </div>
        </div>

        <div class="grid grid-cols-1 xl:grid-cols-3 gap-4 lg:gap-6">
          <section class="xl:col-span-2 bg-card-light dark:bg-card-dark glass-panel rounded-3xl shadow-sm border border-border-light dark:border-border-dark overflow-hidden">
            <div class="p-5 border-b border-border-light dark:border-border-dark"><h3 class="font-black text-lg text-primary dark:text-white">إعدادات وضع الصيانة</h3><p class="text-xs text-gray-500 dark:text-gray-400 mt-1">تحكم مباشر في تفعيل الصيانة وتعديل الرسالة الظاهرة للزوار.</p></div>
            <form method="post" action="/admin/maintenance" class="p-5 space-y-5">
              <label class="flex items-start gap-4 p-4 rounded-2xl bg-white/50 dark:bg-white/5 border border-black/5 dark:border-white/10 cursor-pointer">
                <input type="checkbox" name="maintenance_enabled" value="1" class="mt-1 w-5 h-5 rounded text-primary" <%= enabled ? 'checked' : '' %>>
                <span>
                  <span class="block font-black text-lg text-gray-900 dark:text-white">تفعيل وضع الصيانة للموقع العام</span>
                  <span class="block text-sm text-gray-500 dark:text-gray-400 mt-1">عند التفعيل، سيتم إخفاء الواجهة العامة وإظهار صفحة الصيانة فقط للزوار.</span>
                </span>
              </label>
              <div>
                <label class="block font-black mb-2 text-gray-900 dark:text-white">رسالة الصيانة</label>
                <textarea name="maintenance_message" rows="6" class="field-box w-full rounded-2xl border border-gray-200 dark:border-white/10 bg-white dark:bg-black/20 p-4 font-bold text-gray-800 dark:text-white outline-none"><%= message %></textarea>
                <p class="text-xs text-gray-500 dark:text-gray-400 mt-2">يفضل كتابة رسالة مختصرة وواضحة مثل: الموقع تحت التحديث وسيعود قريباً.</p>
              </div>
              <div class="flex flex-wrap gap-3">
                <button class="flex items-center gap-2 px-6 h-12 rounded-2xl font-bold text-sm bg-primary dark:bg-accent text-white dark:text-black shadow-sm hover:-translate-y-1 transition-transform"><span class="material-symbols-outlined text-[22px]">save</span>حفظ الإعدادات</button>
                <a href="/" class="flex items-center gap-2 px-6 h-12 rounded-2xl font-bold text-sm bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-primary dark:text-accent shadow-sm hover:-translate-y-1 transition-transform"><span class="material-symbols-outlined text-[22px]">preview</span>معاينة الموقع</a>
              </div>
            </form>
          </section>

          <section class="bg-card-light dark:bg-card-dark glass-panel rounded-3xl shadow-sm border border-border-light dark:border-border-dark overflow-hidden">
            <div class="p-5 border-b border-border-light dark:border-border-dark"><h3 class="font-black text-lg text-primary dark:text-white">ملخص الحالة</h3></div>
            <div class="p-5 space-y-4">
              <div class="rounded-2xl p-4 <%= enabled ? 'bg-amber-100 dark:bg-amber-500/10 text-amber-900 dark:text-amber-200 border border-amber-200 dark:border-amber-500/20' : 'bg-green-100 dark:bg-green-500/10 text-green-900 dark:text-green-200 border border-green-200 dark:border-green-500/20' %>">
                <p class="font-black mb-1"><%= enabled ? 'الصيانة مفعلة' : 'الصيانة متوقفة' %></p>
                <p class="text-sm opacity-90"><%= enabled ? 'سيشاهد الزوار صفحة الصيانة حالياً.' : 'الموقع العام متاح للزوار بشكل طبيعي.' %></p>
              </div>
              <div class="rounded-2xl p-4 bg-white/50 dark:bg-white/5 border border-black/5 dark:border-white/10">
                <p class="font-black text-primary dark:text-white mb-2">نصيحة</p>
                <p class="text-sm text-gray-600 dark:text-gray-300 leading-7">قبل تفعيل الصيانة، تأكد من انتهاء العمليات الحساسة مثل رفع الأخبار أو تحديث بيانات الأفراد، حتى لا يتم قطع تجربة الزائر أثناء التصفح.</p>
              </div>
            </div>
          </section>
        </div>
      </div>
    </div>
  </main>

  <aside id="mobileNavDrawer" class="fixed top-0 right-0 h-full w-[320px] max-w-[85vw] bg-card-light dark:bg-card-dark glass-panel border-l border-border-light dark:border-border-dark z-[90] flex flex-col">
    <div class="h-1.5 w-full bg-primary"></div>
    <div class="flex items-center justify-between p-6 border-b border-border-light dark:border-border-dark shrink-0"><div><h1 class="font-display font-bold text-xl leading-none text-primary dark:text-white">عائلة خاشقجي</h1><p class="text-[10px] font-bold text-gray-500 dark:text-accent uppercase tracking-widest mt-1.5">نظام الإدارة</p></div><button id="mobileNavClose" class="w-10 h-10 rounded-full bg-gray-100 dark:bg-white/5 flex items-center justify-center text-gray-500 hover:text-primary dark:hover:text-accent transition-colors" type="button"><span class="material-symbols-outlined text-[24px]">close</span></button></div>
    <nav class="flex-1 overflow-y-auto p-6 space-y-3 text-sm font-bold text-gray-700 dark:text-gray-300">
      <a href="/admin" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">group</span> إدارة الأفراد</a>
      <a href="/admin/pages" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">history_edu</span> إدارة الصفحات</a>
      <a href="/admin/honor" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">workspace_premium</span> السير الذاتية</a>
      <a href="/admin/news" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">newspaper</span> الأخبار</a>
      <a href="/admin/dashboard" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">dashboard</span> لوحة الإحصائيات</a>
      <a href="/admin/activity" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">manage_history</span> سجل النشاطات</a>
      <a href="/admin/maintenance" class="flex items-center gap-3 px-4 py-4 rounded-2xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent border border-primary/20 dark:border-accent/20"><span class="material-symbols-outlined">construction</span> وضع الصيانة</a>
      <a href="/admin/support-messages" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">mail</span> رسائل الدعم</a>
      <div class="my-4 border-t border-border-light dark:border-border-dark"></div>
      <a href="/" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">account_tree</span> العودة للموقع</a>
      <form method="post" action="/admin/logout" class="m-0 mt-4"><button type="submit" class="w-full flex items-center justify-center gap-2 px-4 py-4 rounded-2xl border border-red-200 dark:border-red-900/30 text-red-600 dark:text-red-400 bg-red-50/50 dark:bg-red-500/10 hover:bg-red-50 transition-colors"><span class="material-symbols-outlined">logout</span> تسجيل خروج</button></form>
    </nav>
  </aside>
  <div id="mobileNavOverlay" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-[80] hidden" aria-hidden="true"></div>

  <script>
    (function () { const btn = document.getElementById("themeBtn"); function setTheme(next) { document.documentElement.setAttribute("data-theme", next); document.documentElement.classList.toggle("dark", next === "dark"); localStorage.setItem("theme", next); if (window.initNexus) window.initNexus(); } btn?.addEventListener("click", () => { const cur = document.documentElement.getAttribute("data-theme") || "dark"; setTheme(cur === "dark" ? "light" : "dark"); }); })();
    (function() { const canvas = document.getElementById('nexusCanvas'); if(!canvas) return; const ctx = canvas.getContext('2d'); let width, height, particles = []; const numParticles = window.innerWidth > 1024 ? 110 : 60; let mouseX = -1000, mouseY = -1000; let dotColor, lineColor, connectionColor; function setColors() { const isDark = document.documentElement.classList.contains('dark'); dotColor = isDark ? 'rgba(229, 184, 105, 0.35)' : 'rgba(0, 90, 43, 0.28)'; lineColor = isDark ? 'rgba(229, 184, 105, 0.08)' : 'rgba(0, 90, 43, 0.08)'; connectionColor = isDark ? 'rgba(229, 184, 105, 0.18)' : 'rgba(0, 90, 43, 0.16)'; } function resize() { width = canvas.width = window.innerWidth; height = canvas.height = window.innerHeight; } function initParticles() { particles = []; for (let i=0;i<numParticles;i++) particles.push({ x: Math.random()*width, y: Math.random()*height, vx:(Math.random()-0.5)*0.45, vy:(Math.random()-0.5)*0.45, radius: Math.random()*1.8+1.2 }); } function animate() { ctx.clearRect(0,0,width,height); particles.forEach((p,i)=>{ p.x+=p.vx; p.y+=p.vy; if(p.x<0||p.x>width) p.vx*=-1; if(p.y<0||p.y>height) p.vy*=-1; ctx.beginPath(); ctx.arc(p.x,p.y,p.radius,0,Math.PI*2); ctx.fillStyle=dotColor; ctx.fill(); for(let j=i+1;j<particles.length;j++){ const p2=particles[j]; const dist=Math.hypot(p.x-p2.x,p.y-p2.y); if(dist<110){ ctx.beginPath(); ctx.moveTo(p.x,p.y); ctx.lineTo(p2.x,p2.y); ctx.strokeStyle=lineColor; ctx.lineWidth=1; ctx.stroke(); } } const mouseDist = Math.hypot(p.x-mouseX,p.y-mouseY); if(mouseDist<140){ ctx.beginPath(); ctx.moveTo(p.x,p.y); ctx.lineTo(mouseX,mouseY); ctx.strokeStyle=connectionColor; ctx.lineWidth=1.2; ctx.stroke(); } }); requestAnimationFrame(animate); } window.initNexus = function() { setColors(); }; resize(); setColors(); initParticles(); animate(); window.addEventListener('resize',()=>{ resize(); initParticles(); }); window.addEventListener('mousemove', e => { mouseX=e.clientX; mouseY=e.clientY; }); window.addEventListener('mouseleave',()=>{ mouseX=-1000; mouseY=-1000; }); })();
  </script>

<script>
  (function finalAdminMenuFix() {
    function ready(fn) {
      if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", fn);
      else fn();
    }

    function getParts() {
      return {
        btn: document.getElementById("mobileNavBtn"),
        drawer: document.getElementById("mobileNavDrawer"),
        overlay: document.getElementById("mobileNavOverlay"),
        closeBtn: document.getElementById("mobileNavClose")
      };
    }

    window.openAdminMobileMenu = function (event) {
      if (event) {
        event.preventDefault();
        event.stopPropagation();
      }
      const { drawer, overlay } = getParts();
      if (!drawer || !overlay) return false;

      drawer.classList.add("admin-menu-open", "isOpen", "mobile-menu-open");
      overlay.classList.add("admin-menu-open");
      overlay.classList.remove("hidden");
      document.body.classList.add("admin-menu-lock");

      drawer.style.setProperty("left", "0", "important");
      drawer.style.setProperty("right", "auto", "important");
      drawer.style.setProperty("display", "flex", "important");
      drawer.style.setProperty("visibility", "visible", "important");
      drawer.style.setProperty("opacity", "1", "important");
      drawer.style.setProperty("pointer-events", "auto", "important");
      drawer.style.setProperty("transform", "translate3d(0,0,0)", "important");

      overlay.style.setProperty("display", "block", "important");
      return false;
    };

    window.closeAdminMobileMenu = function (event) {
      if (event) {
        event.preventDefault();
        event.stopPropagation();
      }
      const { drawer, overlay } = getParts();
      if (!drawer || !overlay) return false;

      drawer.classList.remove("admin-menu-open", "isOpen", "mobile-menu-open");
      overlay.classList.remove("admin-menu-open");
      overlay.classList.add("hidden");
      document.body.classList.remove("admin-menu-lock");

      drawer.style.setProperty("transform", "translate3d(-120%,0,0)", "important");
      drawer.style.setProperty("pointer-events", "none", "important");
      overlay.style.setProperty("display", "none", "important");
      return false;
    };

    ready(function () {
      const { btn, drawer, overlay, closeBtn } = getParts();
      if (!btn || !drawer || !overlay) return;

      btn.setAttribute("onclick", "return window.openAdminMobileMenu(event)");
      btn.style.setProperty("pointer-events", "auto", "important");
      btn.style.setProperty("z-index", "100002", "important");

      drawer.style.setProperty("left", "0", "important");
      drawer.style.setProperty("right", "auto", "important");
      drawer.style.setProperty("transform", "translate3d(-120%,0,0)", "important");
      drawer.style.setProperty("pointer-events", "none", "important");
      overlay.style.setProperty("display", "none", "important");

      ["click", "pointerup", "touchend"].forEach(function (evt) {
        btn.addEventListener(evt, window.openAdminMobileMenu, { capture: true, passive: false });
        closeBtn && closeBtn.addEventListener(evt, window.closeAdminMobileMenu, { capture: true, passive: false });
        overlay.addEventListener(evt, window.closeAdminMobileMenu, { capture: true, passive: false });
      });

      drawer.querySelectorAll("a").forEach(function (link) {
        link.addEventListener("click", function () {
          const { drawer, overlay } = getParts();
          drawer && drawer.classList.remove("admin-menu-open", "isOpen", "mobile-menu-open");
          overlay && overlay.classList.remove("admin-menu-open");
          overlay && overlay.classList.add("hidden");
          document.body.classList.remove("admin-menu-lock");
        }, { capture: false });
      });
    });
  })();
</script>

</body>
</html>
