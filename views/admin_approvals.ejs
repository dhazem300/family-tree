<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
  <meta charset="utf-8"/>
  <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
  <title>لوحة الإدارة - مركز الموافقات</title>

  <script src="/theme.js"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Aref+Ruqaa:wght@400;700&family=Tajawal:wght@300;400;500;700;900&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>

  <script>
    tailwind.config = {
      darkMode: "class",
      theme: {
        extend: {
          colors: {
            primary: "#005A2B",
            primaryDark: "#00361A",
            accent: "#E5B869",
            "bg-light": "#F5F3F7",
            "bg-dark": "#050505",
            "card-light": "rgba(255, 255, 255, 0.8)",
            "card-dark": "rgba(15, 12, 25, 0.8)",
            "border-light": "rgba(0, 90, 43, 0.1)",
            "border-dark": "rgba(229, 184, 105, 0.1)",
          },
          fontFamily: {
            display: ["'Aref Ruqaa'", "serif"],
            sans: ["Tajawal", "sans-serif"],
          },
        }
      }
    };
  </script>

  <style type="text/tailwindcss">
    @layer utilities {
      .scrollbar-hide::-webkit-scrollbar { display: none; }
      .scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
      .glass-panel { @apply backdrop-blur-2xl shadow-xl; }
    }
  </style>

  <style>
    :root { color-scheme: light; }
    html.dark { color-scheme: dark; }
    html.theme-switching, html.theme-switching *, html.theme-switching *::before, html.theme-switching *::after {
      transition: none !important;
      animation-duration: 0s !important;
      animation-delay: 0s !important;
    }

    ::-webkit-scrollbar { width: 6px; height: 6px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: #005A2B; border-radius: 10px; }
    html.dark ::-webkit-scrollbar-thumb { background: #E5B869; }

    #nexusCanvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
      pointer-events: none;
    }
    @media (max-width: 1023px) { #nexusCanvas { pointer-events: none !important; } }

    html.dark .dark-toggle-icon-moon { display: none; }
    html:not(.dark) .dark-toggle-icon-sun { display: none; }

    .admin-menu-toggle {
      position: fixed !important;
      width: 1px !important;
      height: 1px !important;
      opacity: 0 !important;
      pointer-events: none !important;
      clip: rect(0 0 0 0) !important;
    }
    #mobileNavBtn {
      cursor: pointer !important;
      -webkit-tap-highlight-color: transparent !important;
      touch-action: manipulation !important;
      position: relative !important;
      z-index: 10002 !important;
      pointer-events: auto !important;
    }
    #mobileNavOverlay {
      display: none !important;
      opacity: 0 !important;
      pointer-events: none !important;
      z-index: 10000 !important;
    }
    #mobileNavDrawer {
      position: fixed !important;
      top: 0 !important;
      left: 0 !important;
      right: auto !important;
      transform: translateX(-115%) !important;
      pointer-events: none !important;
      z-index: 10001 !important;
      transition: transform .32s cubic-bezier(0.16, 1, 0.3, 1) !important;
      display: flex !important;
      visibility: visible !important;
      opacity: 1 !important;
      height: 100vh !important;
      max-height: 100vh !important;
    }
    #adminMenuToggle:checked ~ #mobileNavDrawer {
      transform: translateX(0) !important;
      pointer-events: auto !important;
    }
    #adminMenuToggle:checked ~ #mobileNavOverlay {
      display: block !important;
      opacity: 1 !important;
      pointer-events: auto !important;
    }
    @media (min-width: 1024px) {
      #adminMenuToggle,
      #mobileNavDrawer,
      #mobileNavOverlay { display: none !important; }
    }

    .bg-primary,
    a.bg-primary,
    button.bg-primary {
      background-color: #005A2B !important;
      background-image: none !important;
    }
    .dark .dark\:bg-accent,
    html.dark .dark\:bg-accent {
      background-color: #E5B869 !important;
      background-image: none !important;
    }
    [class*="bg-gradient"] { background-image: none !important; }

    .approval-card {
      transition: transform .25s cubic-bezier(0.16, 1, 0.3, 1), border-color .25s ease, box-shadow .25s ease;
    }
    .approval-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 18px 36px rgba(0, 90, 43, 0.08);
      border-color: rgba(0, 90, 43, 0.18) !important;
    }
    html.dark .approval-card:hover {
      box-shadow: 0 18px 44px rgba(0, 0, 0, 0.65);
      border-color: rgba(229, 184, 105, 0.22) !important;
    }
    .approval-section {
      overflow: hidden;
      border-radius: 2rem;
      border: 1px solid rgba(0,90,43,0.10);
      background: rgba(255,255,255,0.80);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      box-shadow: 0 18px 44px rgba(0,0,0,0.04);
    }
    html.dark .approval-section {
      border-color: rgba(229,184,105,0.10);
      background: rgba(15, 12, 25, 0.80);
      box-shadow: 0 18px 44px rgba(0,0,0,0.45);
    }
    .field-input {
      border-radius: 1rem !important;
      border-color: rgba(0,90,43,0.12) !important;
      background: rgba(255,255,255,.85) !important;
      font-weight: 700;
    }
    html.dark .field-input {
      border-color: rgba(229,184,105,0.14) !important;
      background: rgba(0,0,0,.22) !important;
      color: #fff !important;
    }
  </style>

  <script>
    (function () {
      const saved = localStorage.getItem("theme") || "dark";
      document.documentElement.setAttribute("data-theme", saved);
      document.documentElement.classList.toggle("dark", saved === "dark");
    })();
  </script>
</head>

<body class="bg-bg-light dark:bg-bg-dark text-gray-800 dark:text-gray-200 font-sans h-screen flex overflow-hidden selection:bg-accent selection:text-primaryDark">

  <%
    const pendingUsersList = Array.isArray(pendingUsers) ? pendingUsers : [];
    const linkRequestsList = Array.isArray(linkRequests) ? linkRequests : [];
    const reportsList = Array.isArray(reports) ? reports : [];
    const eventsList = Array.isArray(events) ? events : [];
    const galleryList = Array.isArray(gallery) ? gallery : [];
    const suggestionsList = Array.isArray(suggestions) ? suggestions : [];
    const invitesList = Array.isArray(invites) ? invites : [];
    const isSuperAdmin = Number(admin?.is_super_admin || 0) === 1;
  %>

  <input id="adminMenuToggle" type="checkbox" class="admin-menu-toggle" aria-hidden="true" />

  <canvas id="nexusCanvas"></canvas>
  <div class="fixed inset-0 bg-gradient-to-b from-white/40 to-white/80 dark:from-black/40 dark:to-[#050505]/90 pointer-events-none z-[1]"></div>

  <aside class="relative z-20 hidden lg:flex flex-col w-[280px] h-[calc(100vh-2rem)] my-4 mx-4 bg-card-light dark:bg-card-dark glass-panel rounded-3xl border border-border-light dark:border-border-dark overflow-hidden shrink-0">
    <div class="flex items-center gap-3 p-6 border-b border-border-light dark:border-border-dark shrink-0">
      <div class="w-12 h-12 flex items-center justify-center rounded-2xl bg-primary dark:bg-accent text-white dark:text-black shadow-lg">
        <span class="material-symbols-outlined text-[28px]">shield_person</span>
      </div>
      <div>
        <h1 class="font-display font-bold text-xl leading-none text-primary dark:text-white">عائلة خاشقجي</h1>
        <p class="text-[10px] font-bold text-gray-500 dark:text-accent uppercase tracking-widest mt-1.5">نظام الإدارة المركزية</p>
      </div>
    </div>

    <nav class="flex-1 overflow-y-auto custom-scrollbar p-4 space-y-6">
      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">نظرة عامة</p>
        <a href="/admin/dashboard" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">dashboard</span> لوحة الإحصائيات
        </a>
      </div>

      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">إدارة العائلة والبيانات</p>
        <a href="/admin" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">group</span> إدارة الأفراد
        </a>
        <% if (typeof userCan === "function" && userCan("person_requests")) { %>
        <a href="/admin/person-requests" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">fact_check</span> طلبات الأفراد
        </a>
        <% } %>
        <a href="/admin/pages" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">history_edu</span> إدارة الصفحات
        </a>
        <a href="/admin/honor" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">workspace_premium</span> السير الذاتية
        </a>
        <a href="/admin/news" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">newspaper</span> الأخبار والمناسبات
        </a>
      </div>

      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">المستخدمون والتواصل</p>
        <% if (typeof userCan === "function" && userCan("users")) { %>
        <a href="/admin/users" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">groups</span> مستخدمي الموقع
        </a>
        <% } %>
        <% if (typeof userCan === "function" && userCan("chats")) { %>
        <a href="/admin/chats" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">forum</span> إدارة المحادثات
        </a>
        <% } %>
        <% if (typeof userCan === "function" && (userCan("persons") || userCan("backups") || isSuperAdmin)) { %>
        <a href="/admin/tree-exports" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">file_export</span> سجل تصدير الشجرة
        </a>
        <% } %>
        <a href="/admin/support-messages" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">mail</span> رسائل الدعم
        </a>
      </div>

      <div class="space-y-2">
        <p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">النظام والصلاحيات</p>
        <% if (typeof userCan === "function" && userCan("roles")) { %>
        <a href="/admin/roles" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">manage_accounts</span> وظائف الأفراد
        </a>
        <% } %>
        <% if (isSuperAdmin) { %>
        <a href="/admin/activity" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">manage_history</span> سجل النشاط
        </a>
        <% } %>
        <a href="/admin/approvals" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-bold text-sm border border-primary/20 dark:border-accent/20 transition-all relative overflow-hidden shadow-sm">
          <div class="absolute right-0 top-1/2 -translate-y-1/2 w-1.5 h-6 rounded-l-full bg-primary dark:bg-accent shadow-[0_0_10px_rgba(229,184,105,0.8)]"></div>
          <span class="material-symbols-outlined text-[22px]">verified_user</span> مركز الموافقة
        </a>
        <% if (isSuperAdmin) { %>
        <a href="/admin/maintenance" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">construction</span> وضع الصيانة
        </a>
        <% } %>
        <% if (typeof userCan === "function" && (userCan("backups") || isSuperAdmin)) { %>
        <a href="/admin/backups" class="flex items-center gap-3 px-4 py-3.5 rounded-2xl text-gray-600 dark:text-gray-400 hover:bg-white dark:hover:bg-white/5 hover:text-primary dark:hover:text-accent font-bold text-sm transition-colors border border-transparent">
          <span class="material-symbols-outlined text-[22px]">backup</span> النسخ الاحتياطي
        </a>
        <% } %>
      </div>
    </nav>

    <div class="p-4 border-t border-border-light dark:border-border-dark shrink-0 m-2 rounded-2xl bg-white/50 dark:bg-black/30">
      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-full bg-cover bg-center border border-gray-200 dark:border-white/10 shadow-sm" style="background-image:url('/assets/default-avatar.svg'); background-color: #ddd;"></div>
        <div class="flex-1 min-w-0">
          <p class="text-sm font-bold text-gray-900 dark:text-white truncate"><%= admin?.username || "مدير النظام" %></p>
          <p class="text-[10px] text-primary dark:text-accent uppercase tracking-widest mt-0.5 font-bold">Admin</p>
        </div>
        <form method="post" action="/admin/logout" class="m-0">
          <button type="submit" class="w-9 h-9 flex items-center justify-center rounded-xl bg-red-50 dark:bg-red-500/10 text-red-500 hover:bg-red-500 hover:text-white transition-all shadow-sm" title="تسجيل خروج">
            <span class="material-symbols-outlined text-[18px]">logout</span>
          </button>
        </form>
      </div>
    </div>
  </aside>

  <main class="flex-1 flex flex-col min-w-0 h-full relative z-10">
    <header class="flex items-center justify-between p-4 lg:p-8 shrink-0 gap-4">
      <div class="flex items-center gap-3 min-w-0">
        <label id="mobileNavBtn" for="adminMenuToggle" class="lg:hidden flex items-center justify-center w-12 h-12 rounded-2xl bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-primary dark:text-accent shadow-sm cursor-pointer" aria-controls="mobileNavDrawer" aria-label="فتح القائمة">
          <span class="material-symbols-outlined text-[24px]">menu</span>
        </label>

        <div class="min-w-0">
          <h2 class="text-2xl md:text-3xl font-display font-bold text-primary dark:text-white tracking-tight leading-none mb-1.5">مركز الموافقات</h2>
          <div class="flex items-center gap-1.5 text-gray-500 text-[11px] font-bold uppercase tracking-widest">
            <span>لوحة الإدارة</span>
            <span class="material-symbols-outlined text-[14px]">chevron_left</span>
            <span class="text-accent">الحسابات والطلبات والمحتوى</span>
          </div>
        </div>
      </div>

      <div class="flex items-center gap-3 shrink-0">
        <button id="themeBtn" class="flex items-center justify-center w-12 h-12 rounded-2xl bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-gray-600 dark:text-accent shadow-sm hover:-translate-y-1 transition-transform" type="button" title="تبديل المظهر">
          <span class="material-symbols-outlined text-[22px] dark-toggle-icon-moon">dark_mode</span>
          <span class="material-symbols-outlined hidden dark:block text-[24px] dark-toggle-icon-sun">light_mode</span>
        </button>

        <a href="/admin/users" class="hidden sm:flex items-center gap-2 px-5 h-12 rounded-2xl font-bold text-sm bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark text-primary dark:text-accent shadow-sm hover:-translate-y-1 transition-transform">
          <span class="material-symbols-outlined text-[22px]">groups</span>
          <span class="hidden md:inline">المستخدمون</span>
        </a>

        <a href="/admin" class="hidden sm:flex items-center gap-2 px-5 h-12 rounded-2xl font-bold text-sm bg-primary dark:bg-accent text-white dark:text-black shadow-[0_5px_20px_rgba(0,90,43,0.25)] dark:shadow-[0_5px_20px_rgba(229,184,105,0.20)] hover:-translate-y-1 transition-transform">
          <span class="material-symbols-outlined text-[22px]">group</span>
          <span class="hidden md:inline">إدارة الأفراد</span>
        </a>
      </div>
    </header>

    <div class="flex-1 overflow-y-auto custom-scrollbar px-4 lg:px-8 pb-12">
      <div class="max-w-[100rem] mx-auto space-y-8">

        <section class="grid sm:grid-cols-2 xl:grid-cols-6 gap-4">
          <% const cards = [
            ["حسابات", pendingUsersList.length, "person_add", "#users"],
            ["ربط الشجرة", linkRequestsList.length, "account_tree", "#links"],
            ["بلاغات", reportsList.length, "report", "#reports"],
            ["مناسبات", eventsList.length, "event", "#events"],
            ["صور", galleryList.length, "photo_library", "#gallery"],
            ["تعديلات", suggestionsList.length, "edit_note", "#suggestions"]
          ]; cards.forEach(c => { %>
            <a href="<%= c[3] %>" class="approval-card rounded-[2rem] bg-card-light dark:bg-card-dark glass-panel border border-border-light dark:border-border-dark p-5 shadow-sm group">
              <div class="flex items-center justify-between gap-3 mb-4">
                <div class="w-12 h-12 rounded-2xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent flex items-center justify-center group-hover:scale-110 transition-transform">
                  <span class="material-symbols-outlined text-[25px]"><%= c[2] %></span>
                </div>
                <span class="material-symbols-outlined text-gray-300 dark:text-gray-600 group-hover:text-primary dark:group-hover:text-accent transition-colors">arrow_back</span>
              </div>
              <p class="text-[11px] font-bold text-gray-500 uppercase tracking-widest mb-1"><%= c[0] %></p>
              <p class="text-3xl font-black text-primary dark:text-white leading-none"><%= c[1] %></p>
            </a>
          <% }) %>
        </section>

        <section class="approval-section p-4 md:p-5">
          <div class="flex flex-wrap items-center justify-between gap-3 mb-4">
            <div>
              <h3 class="font-display text-2xl font-bold text-primary dark:text-accent">اختصارات المراجعة</h3>
              <p class="text-sm text-gray-500 font-bold mt-1">انتقل مباشرة إلى نوع الطلب المطلوب مراجعته.</p>
            </div>
          </div>
          <div class="flex flex-wrap gap-2">
            <a href="#users" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">person_add</span> حسابات جديدة</a>
            <a href="#links" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">account_tree</span> ربط الشجرة</a>
            <a href="#reports" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">report</span> البلاغات</a>
            <a href="#events" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">event</span> المناسبات</a>
            <a href="#gallery" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">photo_library</span> الصور</a>
            <a href="#suggestions" class="px-4 h-11 rounded-2xl bg-white dark:bg-black/20 border border-border-light dark:border-border-dark text-primary dark:text-accent font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">edit_note</span> التعديلات</a>
            <a href="#invites" class="px-4 h-11 rounded-2xl bg-primary dark:bg-accent text-white dark:text-black font-black text-sm flex items-center gap-2"><span class="material-symbols-outlined text-[19px]">key</span> أكواد الدعوة</a>
          </div>
        </section>

        <section id="users" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">حسابات جديدة قيد المراجعة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">قبول أو رفض الحسابات الجديدة قبل تفعيلها.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= pendingUsersList.length %></span>
          </div>
          <div class="divide-y divide-gray-100 dark:divide-white/10">
            <% if(!pendingUsersList.length){ %><div class="p-8 text-center text-gray-500 font-bold">لا توجد حسابات جديدة.</div><% } %>
            <% pendingUsersList.forEach(u=>{ %>
              <div class="p-5 flex flex-wrap gap-4 justify-between items-center hover:bg-white/60 dark:hover:bg-white/5 transition-colors">
                <div class="min-w-0">
                  <div class="font-black text-primary dark:text-accent"><%= u.full_name || 'بدون اسم' %> <span class="text-xs text-gray-500">بن <%= u.father_name || '—' %></span></div>
                  <div class="text-sm text-gray-500 break-all" dir="ltr"><%= u.email || '' %> — <%= u.phone || '' %></div>
                  <div class="text-xs text-gray-400 font-bold mt-1">طريقة الدخول: <%= u.provider || 'email' %> — <%= u.created_at || '' %></div>
                </div>
                <div class="flex flex-wrap gap-2">
                  <form method="post" action="/admin/users/<%= u.id %>/approval">
                    <input type="hidden" name="action" value="approve">
                    <button class="h-10 px-4 rounded-xl bg-green-600 text-white font-black shadow-sm hover:opacity-90 transition-opacity">قبول</button>
                  </form>
                  <form method="post" action="/admin/users/<%= u.id %>/approval" class="flex flex-wrap gap-2">
                    <input type="hidden" name="action" value="reject">
                    <input name="reason" placeholder="سبب الرفض" class="field-input h-10 w-40">
                    <button class="h-10 px-4 rounded-xl bg-red-600 text-white font-black shadow-sm hover:opacity-90 transition-opacity">رفض</button>
                  </form>
                </div>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="links" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">طلبات ربط الحساب بالشجرة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">مراجعة طلبات ربط المستخدمين بأفراد الشجرة.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= linkRequestsList.length %></span>
          </div>
          <div class="divide-y divide-gray-100 dark:divide-white/10">
            <% if(!linkRequestsList.length){ %><div class="p-8 text-center text-gray-500 font-bold">لا توجد طلبات ربط.</div><% } %>
            <% linkRequestsList.forEach(r=>{ %>
              <div class="p-5 flex flex-wrap gap-4 justify-between items-center hover:bg-white/60 dark:hover:bg-white/5 transition-colors">
                <div class="min-w-0">
                  <div class="font-black text-gray-900 dark:text-white"><%= r.full_name || 'مستخدم' %> — <span dir="ltr"><%= r.email || '' %></span></div>
                  <div class="text-sm text-gray-500 mt-1">طلب الربط بـ: <%= r.person_name || r.lineage_text || '—' %></div>
                </div>
                <div class="flex gap-2">
                  <form method="post" action="/admin/link-requests/<%= r.id %>/review"><input type="hidden" name="action" value="approve"><button class="h-10 px-4 rounded-xl bg-green-600 text-white font-black">توثيق وربط</button></form>
                  <form method="post" action="/admin/link-requests/<%= r.id %>/review"><input type="hidden" name="action" value="reject"><button class="h-10 px-4 rounded-xl bg-red-600 text-white font-black">رفض</button></form>
                </div>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="reports" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">البلاغات العامة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">مراجعة البلاغات المقدمة من المستخدمين.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= reportsList.length %></span>
          </div>
          <div class="divide-y divide-gray-100 dark:divide-white/10">
            <% if(!reportsList.length){ %><div class="p-8 text-center text-gray-500 font-bold">لا توجد بلاغات.</div><% } %>
            <% reportsList.forEach(r=>{ %>
              <div class="p-5 flex flex-wrap gap-4 justify-between items-start hover:bg-white/60 dark:hover:bg-white/5 transition-colors">
                <div class="min-w-0">
                  <div class="font-black text-gray-900 dark:text-white"><%= r.reason || 'بلاغ' %></div>
                  <div class="text-sm text-gray-500 mt-1">المبلّغ: <%= r.reporter_name || '—' %> — النوع: <%= r.target_type %> #<%= r.target_id %></div>
                  <p class="mt-2 text-sm leading-7 text-gray-700 dark:text-gray-300"><%= r.details || '' %></p>
                </div>
                <form method="post" action="/admin/content/report/<%= r.id %>/review"><input type="hidden" name="action" value="approve"><button class="h-10 px-4 rounded-xl bg-primary text-white dark:bg-accent dark:text-black font-black">تمت المراجعة</button></form>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="events" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">مناسبات بانتظار الموافقة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">نشر أو رفض المناسبات المرسلة من العائلة.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= eventsList.length %></span>
          </div>
          <div class="grid md:grid-cols-2 xl:grid-cols-3 gap-4 p-5">
            <% if(!eventsList.length){ %><div class="text-gray-500 font-bold p-4">لا توجد مناسبات.</div><% } %>
            <% eventsList.forEach(e=>{ %>
              <div class="rounded-3xl bg-white/70 dark:bg-black/30 border border-border-light dark:border-border-dark p-4 approval-card">
                <% if(e.image_url){ %><img loading="lazy" decoding="async" src="<%= e.image_url %>" class="w-full h-40 object-cover rounded-2xl mb-3" alt=""><% } %>
                <h3 class="font-black text-gray-900 dark:text-white"><%= e.title %></h3>
                <p class="text-sm text-gray-500 mt-1"><%= e.event_type || '' %> — <%= e.event_date || '' %></p>
                <p class="text-sm mt-2 leading-7 text-gray-700 dark:text-gray-300"><%= e.description || '' %></p>
                <div class="flex gap-2 mt-3"><form method="post" action="/admin/content/event/<%= e.id %>/review"><input type="hidden" name="action" value="approve"><button class="h-10 px-4 rounded-xl bg-green-600 text-white font-black">نشر</button></form><form method="post" action="/admin/content/event/<%= e.id %>/review"><input type="hidden" name="action" value="reject"><button class="h-10 px-4 rounded-xl bg-red-600 text-white font-black">رفض</button></form></div>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="gallery" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">صور ووثائق بانتظار الموافقة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">مراجعة الصور والوثائق قبل ظهورها.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= galleryList.length %></span>
          </div>
          <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 p-5">
            <% if(!galleryList.length){ %><div class="text-gray-500 font-bold p-4">لا توجد صور.</div><% } %>
            <% galleryList.forEach(g=>{ %>
              <div class="rounded-3xl bg-white/70 dark:bg-black/30 border border-border-light dark:border-border-dark p-4 approval-card">
                <img loading="lazy" decoding="async" src="<%= g.image_url %>" class="w-full h-40 object-cover rounded-2xl mb-3" alt="">
                <h3 class="font-black text-gray-900 dark:text-white"><%= g.title || 'صورة' %></h3>
                <p class="text-sm text-gray-500 mt-1"><%= g.category || '' %></p>
                <div class="flex gap-2 mt-3"><form method="post" action="/admin/content/gallery/<%= g.id %>/review"><input type="hidden" name="action" value="approve"><button class="h-10 px-4 rounded-xl bg-green-600 text-white font-black">نشر</button></form><form method="post" action="/admin/content/gallery/<%= g.id %>/review"><input type="hidden" name="action" value="reject"><button class="h-10 px-4 rounded-xl bg-red-600 text-white font-black">رفض</button></form></div>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="suggestions" class="approval-section">
          <div class="p-5 border-b border-border-light dark:border-border-dark flex items-center justify-between gap-3">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">اقتراحات تعديل بيانات الشجرة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">مراجعة الاقتراحات الخاصة ببيانات الأفراد.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= suggestionsList.length %></span>
          </div>
          <div class="divide-y divide-gray-100 dark:divide-white/10">
            <% if(!suggestionsList.length){ %><div class="p-8 text-center text-gray-500 font-bold">لا توجد اقتراحات.</div><% } %>
            <% suggestionsList.forEach(s=>{ %>
              <div class="p-5 flex flex-wrap justify-between gap-3 hover:bg-white/60 dark:hover:bg-white/5 transition-colors">
                <div class="min-w-0"><div class="font-black text-gray-900 dark:text-white"><%= s.person_name || 'شخص' %> — <%= s.field_name || 'حقل' %></div><div class="text-sm text-gray-500 mt-1">المقترح: <%= s.suggested_value || '' %></div><p class="text-sm mt-2 leading-7 text-gray-700 dark:text-gray-300"><%= s.reason || '' %></p></div>
                <form method="post" action="/admin/content/suggestion/<%= s.id %>/review"><input type="hidden" name="action" value="approve"><button class="h-10 px-4 rounded-xl bg-primary text-white dark:bg-accent dark:text-black font-black">تمت المراجعة</button></form>
              </div>
            <% }) %>
          </div>
        </section>

        <section id="invites" class="approval-section p-5">
          <div class="flex flex-wrap items-center justify-between gap-3 mb-5">
            <div>
              <h2 class="text-xl font-black text-primary dark:text-accent">أكواد الدعوة</h2>
              <p class="text-sm text-gray-500 font-bold mt-1">إنشاء ومتابعة أكواد الدعوة للمستخدمين.</p>
            </div>
            <span class="px-3 py-1 rounded-xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent font-black text-sm"><%= invitesList.length %></span>
          </div>
          <form method="post" action="/admin/invites" class="grid md:grid-cols-5 gap-3 mb-5">
            <input name="code" placeholder="كود أو اتركه تلقائي" class="field-input h-11">
            <input name="note" placeholder="ملاحظة" class="field-input h-11">
            <input name="max_uses" type="number" min="1" value="1" class="field-input h-11">
            <input name="expires_at" type="date" class="field-input h-11">
            <button class="h-11 rounded-2xl bg-primary text-white dark:bg-accent dark:text-black font-black">إنشاء كود</button>
          </form>
          <div class="grid md:grid-cols-3 gap-3">
            <% if(!invitesList.length){ %><div class="text-gray-500 font-bold p-4">لا توجد أكواد دعوة.</div><% } %>
            <% invitesList.forEach(i=>{ %>
              <div class="rounded-2xl bg-white/70 dark:bg-black/30 border border-border-light dark:border-border-dark p-4"><div class="font-black text-primary dark:text-accent" dir="ltr"><%= i.code %></div><div class="text-xs text-gray-500 mt-1">استخدم <%= i.used_count || 0 %> / <%= i.max_uses || 1 %></div><div class="text-xs text-gray-500 mt-1"><%= i.note || '' %></div></div>
            <% }) %>
          </div>
        </section>
      </div>
    </div>
  </main>

  <aside id="mobileNavDrawer" class="fixed top-0 left-0 h-full w-[320px] max-w-[85vw] bg-card-light dark:bg-card-dark glass-panel border-r border-border-light dark:border-border-dark z-[90] flex flex-col">
    <div class="h-1.5 w-full bg-primary"></div>
    <div class="flex items-center justify-between p-6 border-b border-border-light dark:border-border-dark shrink-0">
      <div>
        <h1 class="font-display font-bold text-xl leading-none text-primary dark:text-white">عائلة خاشقجي</h1>
        <p class="text-[10px] font-bold text-gray-500 dark:text-accent uppercase tracking-widest mt-1.5">نظام الإدارة</p>
      </div>
      <label id="mobileNavClose" for="adminMenuToggle" class="w-10 h-10 rounded-full bg-gray-100 dark:bg-white/5 flex items-center justify-center text-gray-500 hover:text-primary dark:hover:text-accent transition-colors cursor-pointer" aria-label="إغلاق القائمة">
        <span class="material-symbols-outlined text-[24px]">close</span>
      </label>
    </div>

    <nav class="flex-1 overflow-y-auto custom-scrollbar p-6 space-y-5 text-sm font-bold text-gray-700 dark:text-gray-300">
      <div class="space-y-2"><p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">نظرة عامة</p><a href="/admin/dashboard" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">dashboard</span> لوحة الإحصائيات</a></div>
      <div class="space-y-2"><p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">إدارة العائلة والبيانات</p><a href="/admin" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">group</span> إدارة الأفراد</a><% if (typeof userCan === "function" && userCan("person_requests")) { %><a href="/admin/person-requests" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">fact_check</span> طلبات الأفراد</a><% } %>
        <a href="/admin/pages" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">history_edu</span> إدارة الصفحات</a>
        <a href="/admin/honor" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">workspace_premium</span> السير الذاتية</a><a href="/admin/news" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">newspaper</span> الأخبار والمناسبات</a></div>
      <div class="space-y-2"><p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">المستخدمون والتواصل</p><% if (typeof userCan === "function" && userCan("users")) { %><a href="/admin/users" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">groups</span> مستخدمي الموقع</a><% } %><% if (typeof userCan === "function" && userCan("chats")) { %><a href="/admin/chats" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">forum</span> إدارة المحادثات</a><% } %><% if (typeof userCan === "function" && (userCan("persons") || userCan("backups") || isSuperAdmin)) { %><a href="/admin/tree-exports" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">file_export</span> سجل تصدير الشجرة</a><% } %><a href="/admin/support-messages" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">mail</span> رسائل الدعم</a></div>
      <div class="space-y-2"><p class="px-3 text-[10px] font-bold text-gray-400 dark:text-gray-500 uppercase tracking-widest mb-3">النظام والصلاحيات</p><% if (typeof userCan === "function" && userCan("roles")) { %><a href="/admin/roles" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">manage_accounts</span> وظائف الأفراد</a><% } %><% if (isSuperAdmin) { %><a href="/admin/activity" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">manage_history</span> سجل النشاط</a><% } %><a href="/admin/approvals" class="flex items-center gap-3 px-4 py-4 rounded-2xl bg-primary/10 dark:bg-accent/10 text-primary dark:text-accent transition-colors border border-primary/20 dark:border-accent/20"><span class="material-symbols-outlined">verified_user</span> مركز الموافقة</a><% if (isSuperAdmin) { %><a href="/admin/maintenance" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">construction</span> وضع الصيانة</a><% } %><% if (typeof userCan === "function" && (userCan("backups") || isSuperAdmin)) { %><a href="/admin/backups" class="flex items-center gap-3 px-4 py-4 rounded-2xl border border-transparent hover:bg-gray-50 dark:hover:bg-white/5 transition-colors"><span class="material-symbols-outlined text-gray-400">backup</span> النسخ الاحتياطي</a><% } %></div>
      <form method="post" action="/admin/logout" class="m-0 mt-4"><button type="submit" class="w-full flex items-center justify-center gap-2 px-4 py-4 rounded-2xl border border-red-200 dark:border-red-900/30 text-red-600 dark:text-red-400 bg-red-50/50 dark:bg-red-500/10 hover:bg-red-50 transition-colors"><span class="material-symbols-outlined">logout</span> تسجيل خروج</button></form>
    </nav>
  </aside>

  <label id="mobileNavOverlay" for="adminMenuToggle" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-[80] hidden" aria-hidden="true"></label>

  <script>
    (function () {
      const btn = document.getElementById("themeBtn");
      let lockTimer = null;
      function setTheme(next) {
        document.documentElement.classList.add("theme-switching");
        document.documentElement.setAttribute("data-theme", next);
        document.documentElement.classList.toggle("dark", next === "dark");
        localStorage.setItem("theme", next);
        if(window.initNexus) window.initNexus();
        clearTimeout(lockTimer);
        lockTimer = setTimeout(() => document.documentElement.classList.remove("theme-switching"), 80);
      }
      btn?.addEventListener("click", () => {
        const cur = document.documentElement.getAttribute("data-theme") || "dark";
        setTheme(cur === "dark" ? "light" : "dark");
      });
    })();

    (function() {
      const canvas = document.getElementById('nexusCanvas');
      if(!canvas) return;
      const ctx = canvas.getContext('2d');
      let width, height, particles = [];
      const numParticles = window.innerWidth > 1024 ? 42 : 18;
      let mouseX = -1000, mouseY = -1000;
      let dotColor, lineColor, connectionColor;
      window.initNexus = function() {
        const isDark = document.documentElement.classList.contains('dark');
        dotColor = isDark ? 'rgba(229, 184, 105, 0.55)' : 'rgba(0, 90, 43, 0.32)';
        lineColor = isDark ? 'rgba(229, 184, 105, 0.16)' : 'rgba(0, 90, 43, 0.09)';
        connectionColor = isDark ? '229, 184, 105' : '0, 90, 43';
      };
      function resize() { width = window.innerWidth; height = window.innerHeight; canvas.width = width; canvas.height = height; }
      function initParticles() {
        particles = [];
        for (let i = 0; i < numParticles; i++) particles.push({ x: Math.random() * window.innerWidth, y: Math.random() * window.innerHeight, vx: (Math.random() - 0.5) * 0.5, vy: (Math.random() - 0.5) * 0.5, radius: Math.random() * 2.8 + 1.2 });
      }
      function draw() {
        ctx.clearRect(0, 0, width, height);
        for (let i = 0; i < particles.length; i++) {
          const p = particles[i];
          p.x += p.vx; p.y += p.vy;
          if (p.x < 0 || p.x > width) p.vx *= -1;
          if (p.y < 0 || p.y > height) p.vy *= -1;
          ctx.beginPath(); ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2); ctx.fillStyle = dotColor; ctx.fill();
          const dxMouse = mouseX - p.x, dyMouse = mouseY - p.y, distMouse = Math.sqrt(dxMouse * dxMouse + dyMouse * dyMouse);
          if (distMouse < 130) { ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(mouseX, mouseY); ctx.strokeStyle = `rgba(${connectionColor}, ${0.6 * (1 - distMouse / 130)})`; ctx.lineWidth = 1; ctx.stroke(); p.x += dxMouse * 0.014; p.y += dyMouse * 0.014; }
          for (let j = i + 1; j < particles.length; j++) {
            const p2 = particles[j], dx = p.x - p2.x, dy = p.y - p2.y, dist = Math.sqrt(dx * dx + dy * dy);
            if (dist < 95) { ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(p2.x, p2.y); ctx.strokeStyle = lineColor; ctx.lineWidth = 0.5; ctx.stroke(); }
          }
        }
        requestAnimationFrame(draw);
      }
      window.addEventListener('resize', () => { resize(); initParticles(); });
      window.addEventListener('mousemove', (e) => { mouseX = e.clientX; mouseY = e.clientY; });
      window.addEventListener('mouseout', () => { mouseX = -1000; mouseY = -1000; });
      window.initNexus(); resize(); initParticles(); draw();
    })();
  </script>
</body>
</html>
